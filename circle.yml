dependencies:
  override:
    - wget https://github.com/boot-clj/boot/releases/download/2.2.0/boot.sh && mv boot.sh boot && chmod a+x boot && sudo mv boot /usr/local/bin
test:
  override:
    - boot test
